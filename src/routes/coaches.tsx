import {pageHead} from "@/lib/seo";
import {createFileRoute,Link} from '@tanstack/react-router';import {getPublicCoaches} from '@/lib/coaching-api';import {PageHero} from '@/components/page-hero';
export const Route=createFileRoute('/coaches')({head:()=>pageHead("/coaches","Meet the Coaches","Meet Oklahoma Prospects instructors and learn about their experience and coaching approach.",false),loader:()=>getPublicCoaches(),component:Coaches,errorComponent:()=> <main id="main" className="mx-auto max-w-3xl px-5 py-12"><h1 className="text-4xl">Meet the coaches</h1><p>Coach profiles could not load. Please try again.</p><Link to="/contact" className="underline">Ask the front desk</Link></main>});
function Coaches(){const rows=Route.useLoaderData();return <main id="main"><PageHero eyebrow="Oklahoma Prospects" title="Meet the coaches." copy="Get to know the instructors working with your family." compact/><section className="mx-auto grid max-w-3xl gap-5 px-5 py-8">{rows.length?rows.map(({id,profile:p})=><article key={id} className="rounded-xl border p-5"><h2 className="text-3xl">{p.name}</h2><p className="mt-2 font-semibold">{p.specialties.join(' · ')}</p>{[['Career & background',p.career],['Approach',p.approach],['Athletes I work with',p.ages],['Accomplishments',p.achievements],['Welcome',p.welcome]].filter(([,v])=>v).map(([label,value])=><div className="mt-4" key={label}><h3 className="text-xl">{label}</h3><p className="mt-1 whitespace-pre-line">{value}</p></div>)}<Link to="/training" className="mt-4 inline-flex min-h-11 items-center underline">Find lessons and assessments</Link></article>):<p>Coach profiles are being prepared. <Link to="/contact" className="underline">Ask the desk about instructors.</Link></p>}</section></main>;}
