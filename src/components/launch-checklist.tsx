import { useEffect,useState } from "react";
import { getCheckoutContext } from "@/lib/commerce/api";
export function LaunchChecklist(){const [mode,setMode]=useState("checking");useEffect(()=>{void getCheckoutContext().then(row=>setMode(row.mode)).catch(()=>setMode("unavailable"));},[]);return <section className="rounded-xl bg-paper-2 p-5"><h3 className="text-2xl">Payment readiness</h3><p className="mt-2" role="status">Stripe: {mode}. {mode!=="live"?"Reserve now, pay at the desk. Live card checkout has not been enabled.":"Secure card checkout is enabled."}</p><p className="mt-2">Ordinary lessons and packages unlock only after coach-confirmed assessment completion. Monthly Development is $239, or $289 for the first month without a completed assessment, then $239 monthly.</p></section>;}
