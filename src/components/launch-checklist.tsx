import { useEffect,useState } from "react";
import { getCheckoutContext } from "@/lib/commerce/api";
export function LaunchChecklist(){const [mode,setMode]=useState("checking");useEffect(()=>{void getCheckoutContext().then(row=>setMode(row.mode)).catch(()=>setMode("unavailable"));},[]);return <section className="rounded-xl bg-paper-2 p-5"><h3 className="text-2xl">Payment readiness</h3><p className="mt-2" role="status">Square: {mode}. {mode!=="live"?"Live card checkout has not been enabled. Payment is required before a booking can be confirmed. Call the front desk for help.":"Secure card checkout is enabled."}</p><p className="mt-2">Ordinary lessons and packages unlock only after coach-confirmed assessment completion. Monthly Development is $229, or $279 for the first month without a completed assessment, then $229 monthly.</p></section>;}
